info:
    namespace: boundedinfinity/example-server
    destDir: /tmp/codegen/example-server-go
    formatSource: false
    templateDump: true
    delimiter: square
schemas:
    -   path: ${SCHEMA_DIR}/codegen-schema/common
    -   path: ${SCHEMA_DIR}/codegen-schema/types
    -   path: ${SCHEMA_DIR}/json-schema/types
templates:    
    files:
        - path: ${TEMPLATES_DIR}/templates/go/types