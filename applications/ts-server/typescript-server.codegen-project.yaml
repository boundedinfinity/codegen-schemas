info:
    namespace: boundedinfinity/example-server
    dest-dir: /tmp/codegen/example-server-ts
    format-source: false
    template-dump: true
    delimiter: square
types:
    -   source-path: ${SCHEMA_DIR}/codegen-schema/common
    -   source-path: ${SCHEMA_DIR}/codegen-schema/types
operations:
    -   source-path: ${SCHEMA_DIR}/codegen-schema/operations
templates:
    types:
        -   source-path: ${TEMPLATES_DIR}/templates/typescript/types