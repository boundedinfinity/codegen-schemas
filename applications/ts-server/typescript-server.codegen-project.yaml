info:
    namespace: boundedinfinity/example-server
    dest-dir: /tmp/codegen/example-server-ts
    format-source: false
    template-dump: true
types:
    - source-path: ${SCHEMA_DIR}/codegen-schema/common
    - source-path: ${SCHEMA_DIR}/codegen-schema/types
    - source-path: ${SCHEMA_DIR}/json-schema/label
templates:    
    files:
        - path: ${TEMPLATES_DIR}/templates/typescript/types