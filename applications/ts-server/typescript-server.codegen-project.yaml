info:
    namespace: boundedinfinity/example-server
    destDir: /tmp/codegen/example-server-ts
    formatSource: false
    templateDump: true
schemas:
    - path: ${SCHEMA_DIR}/codegen-schema/common
    - path: ${SCHEMA_DIR}/codegen-schema/types
    - path: ${SCHEMA_DIR}/json-schema/label
templates:    
    files:
        - path: ${TEMPLATES_DIR}/templates/typescript/types