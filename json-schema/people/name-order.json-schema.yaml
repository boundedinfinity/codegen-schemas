# https://en.wikipedia.org/wiki/Personal_name#Name_order

$id: https://www.boundedinfinity.com/schema/people/name-order
$schema: https://json-schema.org/draft/2020-12/schema
description: A person's name order
type: string
enum:
    -   Given name, Family name
    -   Disabled
    -   Hidden
enum-description:
    Given name, Family name: Western name order
    Family name, Given name: Eastern name order
    
